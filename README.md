# tutorials_nodejs
# install backend dependencies
$ npm install

# start backend server
$ npm run start:dev

# install frontend dependencies
$ cd frontend
$ npm install

# run frontend project
$ ng serve
