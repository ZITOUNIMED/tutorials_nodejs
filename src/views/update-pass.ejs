<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Password</title>
</head>
<style>
    span {
        font-weight: bold;
    }
</style>
<body>
    <main>
        <% if (updated) { %>
            <% if (success) { %>
                <p style="color: green;">Password was updated with success. You can use it to connect now from <a href="http://localhost:4200/connection">here</a>.</p>
            <% } else { %>
                <p style="color: red;">Sorry an error was occured during password update. Please contact <a href="http://localhost:4200/">Customer Service</a>.</p>
            <% } %>
        <% } else { %>
            <form method="POST" action="update">
                <div><span>New Password: <input type="password" id="newPassword" name="newPassword"></span></div>
                <div><span>Confirm Password: <input type="password" id="confirmPassword" name="confirmPassword"></span></div>
                <input type="text" id="login" name="login" value="<%= login %>" hidden>
                <div><button type="submit">Save</button></div>
            </form>
        <% } %>
    </main>
</body>
</html>